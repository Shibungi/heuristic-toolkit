#include <iostream>

int main() {
  // Recommended time split: cheap construction -> local search/SA -> exact repair -> output.
  // Measure quality/iteration, iterations/second, and final score separately before adding complexity.
  std::cerr << "TODO: construct, improve, repair, emit\n";
}
